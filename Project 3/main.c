#include <stdio.h>
#include <stdlib.h>
#include <pthread.h>
#include "queue_utils.h"

int main(int argc, char **argv)
{
	/** Division of work:
	 * Madhuri - Initial program template + adjusted queue utils + report and any remaining work
	 * Vaibhav - Initial thread creation and suspension + queue initialization
	 * Yash - Define sell() function + create threads with sell() function and seller type as arguments
	 * Nachiket - Calculating statistics as mentioned in the PDF
	 * Archana - Define the wakeup_all_seller_threads() function
	 *  **/
	printf("Simulation of multithreading");
	return 0;
}
